# Commonroom SDK

Common Room Community API client, generated from the OpenAPI spec.

List and read Common Room community members.

Learn more about Common Room Community API at [commonroom.io](https://commonroom.io).

This is an unofficial SDK for the Common Room Community public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

> TypeScript, Python, PHP, Golang, Lua, JavaScript SDKs, a CLI with an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

## Entities, not endpoints

This SDK exposes the API as a small set of **semantic entities** — Member — that you
call directly, instead of assembling URL paths and query strings. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `load`):

```ts
const client = new CommonroomSDK()
const items = await client.Member().list({ community_id: "example" })
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
// The offline mock starts EMPTY — seed it with the records the test needs.
// Shape: { entity: { <entity-name>: { <id>: <record> } } }
const client = CommonroomSDK.test({
  entity: {
    member: {
      test01: { id: 'test01' },
    },
  },
})
const members = await client.Member().list()
// members is an array of Member entities, populated with mock data
// — call members[0].data() for the record itself
console.log(members)
```

### Python

```python
client = CommonroomSDK.test()
members = client.Member().list()
print(members)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = CommonroomSDK::test([
    "entity" => ["member" => ["test01" => ["id" => "test01"]]],
]);
$members = $client->Member()->list();
```

### Golang

```go
client := sdk.Test()
result, err := client.Member(nil).List(
    nil, nil,
)
```

### Lua

```lua
local client = sdk.test()
local results, err = client:Member():list()
```

### JavaScript

```js
const client = CommonroomSDK.test()
const members = await client.Member().list()
// members is an array of entities, populated with mock data
// — call members[0].data() for the record itself
console.log(members)
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/commonroom` | publish pending — [install from git tag](https://github.com/voxgig-sdk/commonroom-sdk/releases) |
| Python | `voxgig-sdk-commonroom` | publish pending — [install from git tag](https://github.com/voxgig-sdk/commonroom-sdk/releases) |
| PHP | `voxgig-sdk/commonroom` | publish pending — [install from git tag](https://github.com/voxgig-sdk/commonroom-sdk/releases) |
| Golang | `github.com/voxgig-sdk/commonroom-sdk/go` | `go get github.com/voxgig-sdk/commonroom-sdk/go@latest` |
| Lua | `voxgig-sdk-commonroom` | publish pending — [install from git tag](https://github.com/voxgig-sdk/commonroom-sdk/releases) |
| JavaScript | `@voxgig-sdk/commonroom-js` | publish pending — [install from git tag](https://github.com/voxgig-sdk/commonroom-sdk/releases) |
| Go CLI | `github.com/voxgig-sdk/commonroom-sdk/go-cli` | `go install github.com/voxgig-sdk/commonroom-sdk/go-cli/cmd/commonroom@latest` |
| Go MCP server | `github.com/voxgig-sdk/commonroom-sdk/go-mcp` | `go get github.com/voxgig-sdk/commonroom-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { CommonroomSDK } from '@voxgig-sdk/commonroom'

const client = new CommonroomSDK({
  apikey: process.env.COMMONROOM_APIKEY,
})

// List all members (returns MemberEntity[] — .data() for the record)
const members = await client.Member().list({ community_id: "example" })
for (const member of members) {
  console.log(member)
}
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Lua, JavaScript) | `ts/` `py/` `php/` `go/` `lua/` `js/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o commonroom-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "commonroom": {
      "command": "/abs/path/to/commonroom-mcp"
    }
  }
}
```

## Entities

The API exposes one entity:

| Entity | Description | API path |
| --- | --- | --- |
| **Member** | The Member entity (list, load). | `/{communityId}/members` |

The operations available across these entities are **load**, **list** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from commonroom_sdk import CommonroomSDK

client = CommonroomSDK({
    "apikey": os.environ.get("COMMONROOM_APIKEY"),
})

# List all members (returns a list, raises on error)
members = client.Member().list({"community_id": "example"})
for member in members:
    print(member)

# Load a specific member (returns the record, raises on error)
member = client.Member().load({"id": "example_id", "community_id": "example_community_id"})
print(member)
```

### PHP

```php
<?php
require_once 'commonroom_sdk.php';

$client = new CommonroomSDK([
    "apikey" => getenv("COMMONROOM_APIKEY"),
]);

// List all members (returns an array; throws on error)
$members = $client->Member()->list();
print_r($members);

// Load a specific member (returns the ENTITY; call data_get() for the record; throws on error)
$member = $client->Member()->load(["id" => "example_id", "community_id" => "example_community_id"]);
print_r($member);
```

### Golang

```go
import sdk "github.com/voxgig-sdk/commonroom-sdk/go"

client := sdk.NewCommonroomSDK(map[string]any{
    "apikey": os.Getenv("COMMONROOM_APIKEY"),
})

// List all members
members, err := client.Member(nil).List(nil, nil)
if err != nil {
    panic(err)
}
fmt.Println(members)
```

### Lua

```lua
local sdk = require("commonroom_sdk")

local client = sdk.new({
  apikey = os.getenv("COMMONROOM_APIKEY"),
})

-- List all members
local members, err = client:Member():list()
print(members)

-- Load a specific member
local member, err = client:Member():load({ id = "example_id", community_id = "example_community_id" })
print(member)
```

### JavaScript

```js
const { CommonroomSDK } = require('@voxgig-sdk/commonroom-js')

const client = new CommonroomSDK({
  apikey: process.env.COMMONROOM_APIKEY,
})

// List all members (returns an array)
const members = await client.Member().list({ community_id: "example" })
for (const member of members) {
  console.log(member)
}
```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

**JavaScript:**
```js
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **TestFeature** | In-memory mock transport for testing without a live server |

Pass custom features via the `extend` option at construction time.

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Lua](lua/README.md)
- [JavaScript](js/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

The OpenAPI spec(s) this SDK was generated from are kept in the
[`.sdk/def/`](.sdk/def/) folder.

- Upstream API: [https://api.commonroom.io/community/v1](https://api.commonroom.io/community/v1)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the Common Room Community API OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
